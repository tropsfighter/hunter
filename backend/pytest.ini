[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning

markers =
    endpoint(ep): API route label for coverage report, e.g. "GET /health"
