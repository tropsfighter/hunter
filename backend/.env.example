# YouTube Data API v3 (required for `hunter discover`)
# Create a key in Google Cloud Console and enable YouTube Data API v3.
YOUTUBE_API_KEY=

# Optional: SQLite path (default: backend/data/hunter.db)
# HUNTER_DB_PATH=D:\hunter\hunter\backend\data\hunter.db
